{% set name = "nameparser" %}
{% set version = "1.0.1" %}
{% set sha256 = "ec0ea797e8170d8f46b7abb4190433ac3db03e9eb0a22b46a14e6ba553ce908b" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/derek73/python-{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - nameparser

about:
  home: http://github.com/derek73/python-nameparser
  license: LGPL-2.1+
  license_family: LGPL
  license_file: LICENSE
  summary: 'A simple Python (3.2+ & 2.6+) module for parsing human names into their individual components.'

extra:
  recipe-maintainers:
    - CJ-Wright
